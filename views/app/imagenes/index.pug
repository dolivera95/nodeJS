extends ../../layout.pug
block contenido
	div(class="container")
		h1 Mis imagénes
		for imagen in imagenes
			.white.border.tr.large-padding
				.row
					.col-md-10
						h2=imagen.title
					.col-md-2
						a(href="/app/imagenes/"+imagen.id+"/edit" class="right-space") Editar
						form(action="/app/imagenes/" + imagen.id + "?_method=DELETE" class="link" method="POST")
							input(type="Submit" value="Eliminar")